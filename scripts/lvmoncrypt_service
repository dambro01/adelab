cat > /etc/systemd/system/ade-break.service << EOF
[Unit]
Description=ADE Lab
After=network.target
[Service]
TimeoutStartSec=300s
Type=oneshot
ExecStart=/bin/sh -c 'for i in {1..5}; do /bin/bash /root/Grub.sh; sleep 60; done'
[Install]
WantedBy=multi-user.target
EOF
